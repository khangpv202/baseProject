<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent">

    <include layout="@layout/header_main_setting"
             android:id="@+id/header_layout"/>

    <android.support.v7.widget.RecyclerView
            android:layout_marginLeft="20dp"
            android:layout_marginTop="@dimen/xlarge_margin_size"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@null"
            android:id="@+id/app_list"
            android:layout_below="@+id/header_layout"/>

    <ImageButton android:id="@+id/ib_setting"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:background="@mipmap/ic_launcher"
                 android:layout_marginBottom="10dp"
                 android:layout_above="@+id/bottom_bar"/>
    <ImageButton android:id="@+id/ib_help"
                 android:layout_width="wrap_content"
                 android:layout_height="wrap_content"
                 android:background="@mipmap/ic_launcher"
                 android:layout_toRightOf="@+id/ib_setting"
                 android:layout_marginLeft="7dp"
                 android:layout_marginBottom="10dp"
                 android:layout_above="@+id/bottom_bar"/>

    <include layout="@layout/bottom_main_setting"
             android:id="@+id/bottom_bar"/>

</RelativeLayout>